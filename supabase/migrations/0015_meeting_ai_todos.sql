-- ============================================================
-- HEIM ERP — 미팅 AI 요약에서 추출한 To-do 후보 저장
-- ============================================================
-- AI가 회의록에서 뽑아낸 '대표님 To-do' 액션 아이템을 저장.
-- 사용자가 한 번에/개별적으로 todos 테이블로 추가할 수 있도록 후보 보관.
-- 형식: text[] 를 JSONB 배열로 (예: ["계약서 검토", "투자 미팅 일정"])

ALTER TABLE meetings
  ADD COLUMN IF NOT EXISTS ai_todos JSONB NOT NULL DEFAULT '[]'::jsonb;
